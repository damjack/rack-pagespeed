source 'https://rubygems.org'

group :development do
  gem "bundler", "~> 1.0"
  gem "jeweler", "~> 2.0.1"
end

gem 'nokogiri'
gem 'jsmin'
gem 'mime-types'

group :test do
  gem 'capybara', '1.0.0'
  gem 'redis'
  # gem 'memcached'
end

group :development, :test do
  gem 'rspec',    '2.6.0'
end
